package graph.directedacyclicgraph;


/**
 * Directed acyclic graph -> directed edges and no cycles.
 *
 * All trees structures are DAG because they are not contain cycles
 *
 * SSSP -> Single Source Shortest Path can be solved on DAG in O(V+E) time
 *
 * because graphs can be sorted in a topological ordering via transport and process
 * sequentially.
 */
class Dag {
    public static void main(String[] args) {

    }
}
